#!/usr/bin/env bash

bundle install
bundle exec rake build
ls -t pkg | head -n1 | xargs -I {} mv pkg/{} features/repositories/ruby-app/retest.gem
docker-compose -f features/repositories/ruby-app/docker-compose.yml up