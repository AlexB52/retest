#!/usr/bin/env ruby

require 'retest'

$stdout.sync = true

puts "Launching Retest..."

command = Retest::Options.command(ARGV)

Retest
  .build(runner: Retest::Runner.for(command))
  .start # not blocking

puts "Ready to refactor! You can make file changes now"

sleep
