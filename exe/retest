#!/usr/bin/env ruby

require 'retest'

$stdout.sync = true

options = Retest::Options.new(ARGV)

if options.help?
  $stdout.puts options.help
  return
end

Retest.start(options.command) # not blocking

sleep
