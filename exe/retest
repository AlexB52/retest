#!/usr/bin/env ruby

require 'retest'

$stdout.sync = true

options = Retest::Options.new(ARGV)

return $stdout.puts options.help if options.help?

Retest.start(options.command) # not blocking

sleep
